package com.smile.blog.config;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.Map;
import java.util.stream.Collectors;
import java.util.stream.Stream;

/**
 * @author: Smile
 * @description:
 * @create: 2020-04-24 10:16
 */
public class Test {
    public static void main(String[] args) {
//        Map<Boolean, List<String>> collect = Stream.of("张三", "李四12312", "5555")
//                .distinct().limit(2)
//                .collect(Collectors.partitioningBy(s->s.length()>3));
//        List<String> strings = collect.get(true);
//        System.out.println(strings);
//        String[] strings={"1","2","3","4"};
//        Object[] objects = Stream.of(strings).map(x -> Integer.parseInt(x)).toArray();
//        System.out.println(Arrays.toString(objects));
    }
}
